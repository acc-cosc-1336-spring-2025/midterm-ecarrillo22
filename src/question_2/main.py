#add import
import question_b

def main():

    print(f"The global variable's value is {question_b.global_value}.")
    print(f"The new value of the global variable is {question_b.use_global()}.")

main()